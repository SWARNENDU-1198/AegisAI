---
phase: 01-database-crawler-optimization
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/file_service.py
autonomous: true
requirements:
  - CORE-05
must_haves:
  truths:
    - "Database insertions avoid N+1 SELECT loops and use bulk inserts"
  artifacts:
    - path: "backend/services/file_service.py"
      provides: "Optimized database save logic"
      contains: "bulk_insert_mappings"
---

<objective>
Optimize database file record persistence to eliminate the N+1 query bottleneck.

Purpose: Solve database performance slowdowns when saving thousands of scanned files.
Output: Refactored `backend/services/file_service.py` utilizing SQLAlchemy bulk insert mappings and a set-based path check.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-database-crawler-optimization/01-CONTEXT.md
@backend/services/file_service.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement set-based existence check and bulk insert in file_service.py</name>
  <files>backend/services/file_service.py</files>
  <read_first>backend/services/file_service.py</read_first>
  <action>
    Modify `save_files` in `backend/services/file_service.py` to:
    1. Fetch all existing file paths from the SQLite database in a single query: `existing_paths = {p[0] for p in db.query(FileRecord.path).all()}`.
    2. Filter out items from the input `file_list` whose path already exists in `existing_paths`.
    3. Use `db.bulk_insert_mappings(FileRecord, to_insert)` to save the new records to the database.
    4. Commit the transaction and close the session properly.
  </action>
  <verify>
    Create a temporary test script `backend/test_db_save.py` that calls `save_files` with a list of duplicate and unique dummy files and verify that only unique files are saved.
  </verify>
  <acceptance_criteria>
    - file_service.py contains `bulk_insert_mappings`
    - file_service.py contains `existing_paths = {p[0] for p in db.query(FileRecord.path).all()}`
  </acceptance_criteria>
  <done>Database file persistence works and uses bulk insert</done>
</task>
</tasks>

<verification>
- [ ] Temporary script `backend/test_db_save.py` runs successfully and confirms bulk insertion works correctly without N+1 loops.
</verification>

<success_criteria>
- All tasks completed
- Database insertions optimized using a single query check and bulk insertions
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-crawler-optimization/01-01-SUMMARY.md`
</output>
