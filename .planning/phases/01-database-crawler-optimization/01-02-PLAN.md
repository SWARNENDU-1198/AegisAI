---
phase: 01-database-crawler-optimization
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - backend/main.py
autonomous: true
requirements:
  - CORE-06
must_haves:
  truths:
    - "API request /scan-and-save returns immediately with a task ID and runs in background"
    - "API request /scan-status/{task_id} returns active scan progress details"
    - "Event loop is not blocked during background scans and hashing tasks"
  artifacts:
    - path: "backend/main.py"
      provides: "FastAPI endpoints and background task manager"
      contains:
        - "/scan-status/{task_id}"
        - "asyncio.create_task"
        - "asyncio.to_thread"
---

<objective>
Implement non-blocking background crawler and hashing tasks using Python asyncio.

Purpose: Keep the FastAPI server responsive while processing large file systems.
Output: Refactored `/scan-and-save` endpoint, a new `/scan-status/{task_id}` endpoint, and an asynchronous background runner.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-database-crawler-optimization/01-CONTEXT.md
@backend/main.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Refactor main.py to support background task execution and status tracking</name>
  <files>backend/main.py</files>
  <read_first>backend/main.py</read_first>
  <action>
    Modify `backend/main.py` to:
    1. Import `asyncio`, `uuid`, and `threading` (or use dict for state).
    2. Define a global `scan_state` dictionary and a lock for safety:
       ```python
       scan_state = {
           "task_id": None,
           "status": "idle",
           "total_files": 0,
           "processed_files": 0,
           "current_file": None,
           "error": None
       }
       scan_lock = threading.Lock()
       ```
    3. Define an `async def background_scan_task(task_id: str)` helper function that:
       - Scans directories using `asyncio.to_thread(scan_multiple_directories, SCAN_PATHS)`.
       - Updates `total_files` in the state.
       - Hashing loop: For each file, computes hash using `await asyncio.to_thread(calculate_hash, file["path"])` and updates progress (`processed_files` and `current_file`).
       - Saves files using `await asyncio.to_thread(save_files, files)`.
       - Updates state status to `"completed"` when done, or `"failed"` with the error message if an exception occurs.
    4. Refactor the `@app.get("/scan-and-save")` endpoint to:
       - Check if `scan_state["status"] == "running"`. If so, return a JSON response (with HTTP status 409) indicating a scan is already running.
       - Generate a unique task ID using `uuid.uuid4()`.
       - Reset `scan_state` with the new task ID and status `"running"`.
       - Call `asyncio.create_task(background_scan_task(task_id))` to run it in the background.
       - Return `{"task_id": task_id, "message": "Scan started in background"}` immediately.
    5. Add a new `@app.get("/scan-status/{task_id}")` endpoint to return the current progress.
  </action>
  <verify>
    Create a test script `backend/test_bg_scan.py` that hits `/scan-and-save` to start a background scan, then polls `/scan-status/{task_id}` to verify progress reporting works and server remains responsive.
  </verify>
  <acceptance_criteria>
    - main.py contains `asyncio.to_thread`
    - main.py contains `asyncio.create_task`
    - main.py contains `@app.get("/scan-status/{task_id}")`
  </acceptance_criteria>
  <done>Background scanning and status tracking endpoint implemented and verified</done>
</task>
</tasks>

<verification>
- [ ] Test script `backend/test_bg_scan.py` executes successfully, starts background scan, polls progress, and confirms non-blocking behaviour.
</verification>

<success_criteria>
- All tasks completed
- Scan endpoint is non-blocking and returns status info
- Progress tracking endpoint returns correct statistics
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-crawler-optimization/01-02-SUMMARY.md`
</output>
