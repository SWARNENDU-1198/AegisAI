---
phase: 03-rich-metadata-engine
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/database/models.py
  - backend/database/init_db.py
  - backend/services/file_service.py
  - backend/main.py
autonomous: true
requirements:
  - META-01
  - META-02
  - META-03
must_haves:
  truths:
    - "Database schema contains a 'meta_data' column in files table"
    - "Libraries pypdf and tinytag are installed"
    - "Background scan extracts metadata for images, audio, PDFs, and Office docs"
    - "/file-metadata/{id} returns the extracted metadata JSON for that file record"
  artifacts:
    - path: "backend/services/metadata_service.py"
      provides: "Logical metadata extraction for images, audio, PDF, and Office files"
      contains:
        - "extract_metadata"
    - path: "backend/test_metadata.py"
      provides: "Automated unit and integration testing suite for Phase 3"
---

<objective>
Implement the Rich Metadata Engine for AegisAI.
1. Add `meta_data` column to the database schema and enable safe auto-migration.
2. Install pip dependencies `pypdf` and `tinytag`.
3. Build the `metadata_service.py` to extract dimensions/creation date (Images), duration/artist (Audio), pages/author (PDFs and Office).
4. Integrate metadata extraction into the background crawl scanner.
5. Expose `/file-metadata/{id}` GET endpoint.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-rich-metadata-engine/03-CONTEXT.md
@.planning/phases/03-rich-metadata-engine/03-RESEARCH.md
@backend/database/models.py
@backend/database/init_db.py
@backend/services/file_service.py
@backend/main.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Update database model and migration for meta_data column</name>
  <files>
    - backend/database/models.py
    - backend/database/init_db.py
  </files>
  <read_first>
    - backend/database/models.py
    - backend/database/init_db.py
  </read_first>
  <action>
    1. In `backend/database/models.py`, add:
       ```python
       meta_data = Column(String, nullable=True)
       ```
    2. In `backend/database/init_db.py`, update `create_tables()` to check for `meta_data` column and run migration if not present:
       ```python
       if "meta_data" not in columns:
           conn.execute(text("ALTER TABLE files ADD COLUMN meta_data TEXT"))
           conn.commit()
       ```
  </action>
  <verify>
    Verify by importing models and running init_db.
  </verify>
  <acceptance_criteria>
    - models.py contains `meta_data = Column(String`
    - init_db.py contains `ALTER TABLE files ADD COLUMN meta_data TEXT`
  </acceptance_criteria>
  <done>Database schema updated and migration added for meta_data column</done>
</task>

<task type="auto">
  <name>Task 2: Install dependencies pypdf and tinytag</name>
  <files>
    - venv
  </files>
  <action>
    Run pip command to install `pypdf` and `tinytag`:
    ```bash
    venv\Scripts\pip.exe install pypdf tinytag
    ```
  </action>
  <verify>
    Run `pip show pypdf tinytag` to verify installation.
  </verify>
  <acceptance_criteria>
    - pypdf and tinytag are successfully installed in the virtual environment.
  </acceptance_criteria>
  <done>Dependencies pypdf and tinytag installed</done>
</task>

<task type="auto">
  <name>Task 3: Implement Rich Metadata Extraction Service</name>
  <files>
    - backend/services/metadata_service.py [NEW]
  </files>
  <action>
    Create `backend/services/metadata_service.py` containing:
    1. `extract_image_metadata(file_path: str) -> dict` returning `width`, `height`, and `creation_date`.
    2. `extract_audio_metadata(file_path: str) -> dict` returning `artist` and `duration` (using `tinytag`).
    3. `extract_pdf_metadata(file_path: str) -> dict` returning `author` and `pages` (using `pypdf`).
    4. `extract_office_metadata(file_path: str) -> dict` returning `author` and `pages` (using zip properties).
    5. `extract_metadata(file_path: str, category: str, extension: str) -> dict` dispatcher mapping category/extension to appropriate extractors.
  </action>
  <verify>
    We will write tests in test_metadata.py verifying image, audio, PDF, and Office document metadata extraction.
  </verify>
  <acceptance_criteria>
    - metadata_service.py is created and exports `extract_metadata`.
  </acceptance_criteria>
  <done>Rich metadata extraction service implemented</done>
</task>

<task type="auto">
  <name>Task 4: Integrate metadata extraction into scanner and database saves</name>
  <files>
    - backend/services/file_service.py
    - backend/main.py
  </files>
  <read_first>
    - backend/services/file_service.py
    - backend/main.py
  </read_first>
  <action>
    1. Update `backend/services/file_service.py` to insert `"meta_data": file.get("meta_data")` in bulk mappings.
    2. Update `backend/main.py` background scanner loop to call `extract_metadata(file["path"], file["category"], file["extension"])` via `asyncio.to_thread` and save the serialized JSON into `file["meta_data"]`.
  </action>
  <verify>
    Verify background scan runs successfully and populates `meta_data` field in DB.
  </verify>
  <acceptance_criteria>
    - file_service.py includes `"meta_data"` in insert mapping.
    - main.py background task updates file records with serialized JSON metadata.
  </acceptance_criteria>
  <done>Metadata extraction integrated into background scanning and db bulk inserts</done>
</task>

<task type="auto">
  <name>Task 5: Implement /file-metadata/{id} GET endpoint</name>
  <files>
    - backend/main.py
  </files>
  <read_first>
    - backend/main.py
  </read_first>
  <action>
    In `backend/main.py`:
    - Add route `@app.get("/file-metadata/{file_id}")` fetching the `FileRecord` from DB.
    - If record is not found, raise 404.
    - Parse `meta_data` string using `json.loads` if present, else return `{}` or default fields.
  </action>
  <verify>
    Test /file-metadata/{id} route manually or via automated test.
  </verify>
  <acceptance_criteria>
    - GET `/file-metadata/{id}` returns parsed JSON metadata dict.
  </acceptance_criteria>
  <done>File metadata GET endpoint exposed</done>
</task>

<task type="auto">
  <name>Task 6: Create test suite test_metadata.py and verify correctness</name>
  <files>
    - backend/test_metadata.py [NEW]
  </files>
  <action>
    Create unit and integration test suite `backend/test_metadata.py` verifying all aspects of metadata parsing, background scanning integration, and endpoint correctness.
  </action>
  <verify>
    Run test suite: `venv\Scripts\python.exe -m unittest backend/test_metadata.py`
  </verify>
  <acceptance_criteria>
    - All tests in `test_metadata.py` pass.
  </acceptance_criteria>
  <done>Metadata test suite implemented and all tests pass</done>
</task>
</tasks>

<verification>
- [ ] Test suite `backend/test_metadata.py` executes successfully.
- [ ] Integration test confirms metadata extraction from temp image, PDF, ZIP-office files, and endpoint response.
</verification>

<success_criteria>
- SQLite migration successfully runs to add `meta_data` column.
- Scanner saves extracted properties to the database without performance overhead.
- HTTP API returns metadata correct for the media/document types requested.
</success_criteria>

<output>
Create `.planning/phases/03-rich-metadata-engine/03-01-SUMMARY.md` on completion.
</output>
