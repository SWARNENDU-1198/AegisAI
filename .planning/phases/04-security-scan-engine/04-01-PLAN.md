---
phase: 04-security-scan-engine
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/metadata_service.py
  - backend/main.py
autonomous: true
requirements:
  - SEC-01
  - SEC-02
must_haves:
  truths:
    - "Security scan identifies plain-text passwords, API keys, and PII"
    - "Security scan alerts users to dangerous executables (.exe, .bat, .ps1, etc.)"
    - "/security-report GET endpoint returns findings grouped by alert type"
  artifacts:
    - path: "backend/services/security_service.py"
      provides: "Identifies secrets and unverified executable alerts"
      contains:
        - "analyze_file_security"
    - path: "backend/test_security.py"
      provides: "Unit and integration tests for security scanner"
---

<objective>
Implement the Security Scan Engine for AegisAI.
1. Build `security_service.py` to check for plain-text secrets and dangerous scripts/executables.
2. Integrate it into `metadata_service.py` dispatcher to store findings inside `meta_data` JSON.
3. Expose the `/security-report` GET endpoint returning grouped security alerts.
4. Implement `test_security.py` and pass tests.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-security-scan-engine/04-CONTEXT.md
@.planning/phases/04-security-scan-engine/04-RESEARCH.md
@backend/main.py
@backend/services/metadata_service.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build Security Service</name>
  <files>
    - backend/services/security_service.py [NEW]
  </files>
  <action>
    Create `backend/services/security_service.py` containing:
    1. Regex patterns for api-keys, passwords, credit-cards, SSNs.
    2. List of dangerous extensions: `.exe`, `.bat`, `.ps1`, `.vbs`, `.sh`, `.cmd`, `.msi`.
    3. `analyze_file_security(file_path: str, extension: str) -> list[str]` which:
       - Flags dangerous extension files as `unverified_executable`.
       - Reads files under 1MB with text-like extensions and scans line-by-line for regex secrets.
       - Returns a list of alert strings: e.g. `["contains_api_key", "unverified_executable"]`.
  </action>
  <verify>
    Write tests in test_security.py to verify scanning.
  </verify>
  <acceptance_criteria>
    - security_service.py exports `analyze_file_security`.
  </acceptance_criteria>
  <done>Security service implemented</done>
</task>

<task type="auto">
  <name>Task 2: Integrate security scanner into metadata dispatcher</name>
  <files>
    - backend/services/metadata_service.py
  </files>
  <read_first>
    - backend/services/metadata_service.py
  </read_first>
  <action>
    1. Import `analyze_file_security` from `services.security_service`.
    2. Inside `extract_metadata(file_path, category, extension)` in `backend/services/metadata_service.py`:
       - Run `alerts = analyze_file_security(file_path, extension)`.
       - If `alerts` is not empty, add it to the returned metadata dict as `"security_alerts": alerts`.
  </action>
  <verify>
    Ensure existing tests in test_metadata.py still pass.
  </verify>
  <acceptance_criteria>
    - metadata_service.py integrates `analyze_file_security` and includes `security_alerts` in returned metadata dict.
  </acceptance_criteria>
  <done>Security scanner integrated with metadata service</done>
</task>

<task type="auto">
  <name>Task 3: Expose /security-report GET route</name>
  <files>
    - backend/main.py
  </files>
  <read_first>
    - backend/main.py
  </read_first>
  <action>
    In `backend/main.py`:
    - Add route `@app.get("/security-report")` which:
      - Fetches all file records from DB where `meta_data` is not null.
      - Decodes the JSON `meta_data` and checks for `security_alerts`.
      - Groups results by alert type (e.g. `contains_api_key`, `contains_password`, `unverified_executable`).
      - Returns the grouped dict.
  </action>
  <verify>
    Verify endpoint output matches spec.
  </verify>
  <acceptance_criteria>
    - GET `/security-report` returns grouped findings list.
  </acceptance_criteria>
  <done>Security report endpoint exposed</done>
</task>

<task type="auto">
  <name>Task 4: Implement test_security.py and verify correctness</name>
  <files>
    - backend/test_security.py [NEW]
  </files>
  <action>
    Create unit and integration test suite `backend/test_security.py` verifying all aspects of security parsing, main scan integration, and API endpoint correctness.
  </action>
  <verify>
    Run test suite: `venv\Scripts\python.exe -m unittest backend/test_security.py`
  </verify>
  <acceptance_criteria>
    - All tests in `test_security.py` pass.
  </acceptance_criteria>
  <done>Security test suite implemented and all tests pass</done>
</task>
</tasks>

<verification>
- [ ] Test suite `backend/test_security.py` executes successfully.
</verification>

<success_criteria>
- Scanner identifies dangerous scripts and plain-text credentials.
- Findings are persisted in the database meta_data column.
- Endpoints return correct alerts list.
</success_criteria>

<output>
Create `.planning/phases/04-security-scan-engine/04-01-SUMMARY.md` on completion.
</output>
