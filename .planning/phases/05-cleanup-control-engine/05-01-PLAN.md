---
phase: 05-cleanup-control-engine
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/main.py
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
must_haves:
  truths:
    - "/delete-duplicate endpoint deletes duplicate files safely only on explicit confirmation"
    - "System prevents deleting the last remaining copy of a file hash group"
    - "/cleanup-temp endpoint deletes all Temp files and database records safely on confirmation"
  artifacts:
    - path: "backend/test_cleanup.py"
      provides: "Unit and integration tests for duplicate and temp cleanup functions"
---

<objective>
Implement the Cleanup Control Engine for AegisAI.
1. Implement the `/delete-duplicate` POST endpoint requiring confirmation and checking safety boundaries.
2. Implement the `/cleanup-temp` POST endpoint to bulk-delete temp files on confirmation.
3. Write `test_cleanup.py` and pass tests.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cleanup-control-engine/05-CONTEXT.md
@.planning/phases/05-cleanup-control-engine/05-RESEARCH.md
@backend/main.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement /delete-duplicate endpoint</name>
  <files>
    - backend/main.py
  </files>
  <read_first>
    - backend/main.py
  </read_first>
  <action>
    In `backend/main.py`, define a POST endpoint `/delete-duplicate` accepting a JSON payload or query parameters: `path: str`, `duplicate_hash: str`, `confirm: bool = False`.
    If `confirm` is `False`, raise 400 with message "Confirmation parameter is required".
    Query the record matching `path` and `hash`. If not found, raise 404.
    Count records matching `duplicate_hash`. If count <= 1, raise 400 with "Cannot delete last remaining copy of this file."
    Execute:
    1. Delete the file from the local disk (`os.remove`).
    2. Delete the record from SQLite database.
    Return `{"status": "deleted", "path": path, "recovered_bytes": record.size}`.
  </action>
  <verify>
    Verify with unit test.
  </verify>
  <acceptance_criteria>
    - /delete-duplicate route exists.
    - Prevents deletion if confirm=False or if target is the last copy.
  </acceptance_criteria>
  <done>Duplicate deletion endpoint implemented with safety validation</done>
</task>

<task type="auto">
  <name>Task 2: Implement /cleanup-temp endpoint</name>
  <files>
    - backend/main.py
  </files>
  <read_first>
    - backend/main.py
  </read_first>
  <action>
    In `backend/main.py`, define a POST endpoint `/cleanup-temp` accepting query parameter `confirm: bool = False`.
    If `confirm` is `False`, raise 400.
    Query all records where `category == "Temp"`.
    Iterate, remove each file from disk, and delete the record from DB.
    Return count of files deleted and total bytes size.
  </action>
  <verify>
    Verify with unit test.
  </verify>
  <acceptance_criteria>
    - /cleanup-temp route exists.
    - Deletes all Temp files from disk and database when confirm=True.
  </acceptance_criteria>
  <done>Temp files cleanup endpoint implemented</done>
</task>

<task type="auto">
  <name>Task 3: Implement test_cleanup.py and verify correctness</name>
  <files>
    - backend/test_cleanup.py [NEW]
  </files>
  <action>
    Create unit and integration test suite `backend/test_cleanup.py` verifying all aspects of deletion safety checks, API responses, and database updates.
  </action>
  <verify>
    Run test suite: `venv\Scripts\python.exe -m unittest backend/test_cleanup.py`
  </verify>
  <acceptance_criteria>
    - All tests in `test_cleanup.py` pass.
  </acceptance_criteria>
  <done>Cleanup test suite implemented and all tests pass</done>
</task>
</tasks>

<verification>
- [ ] Test suite `backend/test_cleanup.py` executes successfully.
</verification>

<success_criteria>
- Endpoints enforce explicit user confirmation parameter.
- System prevents deleting the last remaining copy of a duplicate group.
- Disk files and DB records are cleanly synchronized post-deletion.
</success_criteria>

<output>
Create `.planning/phases/05-cleanup-control-engine/05-01-SUMMARY.md` on completion.
</output>
