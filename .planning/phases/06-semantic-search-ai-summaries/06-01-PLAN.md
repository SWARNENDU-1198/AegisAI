---
phase: 06-semantic-search-ai-summaries
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/database/models.py
  - backend/database/init_db.py
  - backend/ai/gemini.py
  - backend/services/file_service.py
  - backend/main.py
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SUMM-01
must_haves:
  truths:
    - "Database schema contains a 'vector_embedding' column in files table"
    - "Gemini client generates vector embeddings using models/text-embedding-004"
    - "/semantic-search GET endpoint returns top similar files using native cosine similarity calculation"
    - "/folder-summary GET endpoint returns generative LLM summary of directory files context"
  artifacts:
    - path: "backend/services/semantic_service.py"
      provides: "Text formulation, vector serialization, and cosine similarity calculation"
      contains:
        - "cosine_similarity"
        - "get_file_text_representation"
    - path: "backend/test_semantic_search.py"
      provides: "Unit and integration testing suite for Phase 6"
---

<objective>
Implement Semantic Search & AI Directory Summarization for AegisAI.
1. Add `vector_embedding` column to database schema and auto-migration checks.
2. Extend `backend/ai/gemini.py` with the embedding client.
3. Build `semantic_service.py` for representation formatting and cosine similarity calculations.
4. Integrate embedding generation into background crawl scanning and DB persistence.
5. Expose `/semantic-search` GET endpoint.
6. Expose `/folder-summary` GET endpoint.
7. Write and pass all test cases in `test_semantic_search.py`.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-semantic-search-ai-summaries/06-CONTEXT.md
@.planning/phases/06-semantic-search-ai-summaries/06-RESEARCH.md
@backend/database/models.py
@backend/database/init_db.py
@backend/ai/gemini.py
@backend/services/file_service.py
@backend/main.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Update database model and migration for vector_embedding column</name>
  <files>
    - backend/database/models.py
    - backend/database/init_db.py
  </files>
  <read_first>
    - backend/database/models.py
    - backend/database/init_db.py
  </read_first>
  <action>
    1. In `backend/database/models.py`, add:
       ```python
       vector_embedding = Column(String, nullable=True)
       ```
    2. In `backend/database/init_db.py`, update `create_tables()` to check for `vector_embedding` column and run migration if not present:
       ```python
       if "vector_embedding" not in columns:
           conn.execute(text("ALTER TABLE files ADD COLUMN vector_embedding TEXT"))
           conn.commit()
       ```
  </action>
  <verify>
    Verify models load and tables init without errors.
  </verify>
  <acceptance_criteria>
    - models.py contains `vector_embedding = Column(String`
    - init_db.py contains `ALTER TABLE files ADD COLUMN vector_embedding TEXT`
  </acceptance_criteria>
  <done>Database schema updated and migration added for vector_embedding column</done>
</task>

<task type="auto">
  <name>Task 2: Build Semantic Search Service and AI Embedding Wrapper</name>
  <files>
    - backend/ai/gemini.py
    - backend/services/semantic_service.py [NEW]
  </files>
  <read_first>
    - backend/ai/gemini.py
  </read_first>
  <action>
    1. In `backend/ai/gemini.py`, add:
       ```python
       def get_embedding(text: str, is_query: bool = False) -> list[float]:
           task_type = "retrieval_query" if is_query else "retrieval_document"
           try:
               response = genai.embed_content(
                   model="models/text-embedding-004",
                   content=text,
                   task_type=task_type
               )
               return response.get("embedding", [])
           except Exception as e:
               logger.error("Failed to generate embedding: %s", str(e))
               return []
       ```
    2. Create `backend/services/semantic_service.py` containing:
       - `cosine_similarity(v1: list[float], v2: list[float]) -> float` native calculation.
       - `get_file_text_representation(file: dict) -> str` returns a descriptive string combining name, path, extension, category, and metadata.
  </action>
  <verify>
    Verify with test_semantic_search.py.
  </verify>
  <acceptance_criteria>
    - gemini.py implements `get_embedding`.
    - semantic_service.py exports `cosine_similarity` and `get_file_text_representation`.
  </acceptance_criteria>
  <done>AI embedding wrapper and semantic service implemented</done>
</task>

<task type="auto">
  <name>Task 3: Integrate embedding generation into scanner and database saves</name>
  <files>
    - backend/services/file_service.py
    - backend/main.py
  </files>
  <read_first>
    - backend/services/file_service.py
    - backend/main.py
  </read_first>
  <action>
    1. Update `backend/services/file_service.py` to map `"vector_embedding": file.get("vector_embedding")` in bulk mappings.
    2. In `backend/main.py` background scanner loop:
       - Import `get_file_text_representation` from `services.semantic_service` and `get_embedding` from `ai.gemini`.
       - After extracting metadata, construct text representation and fetch the vector embedding using `await asyncio.to_thread(get_embedding, text_repr)`.
       - Save the serialized JSON list to `file["vector_embedding"] = json.dumps(embedding)`.
  </action>
  <verify>
    Verify background scan runs successfully and populates `vector_embedding` field.
  </verify>
  <acceptance_criteria>
    - file_service.py includes `"vector_embedding"` in mapping.
    - main.py background task saves vector embedding JSON string.
  </acceptance_criteria>
  <done>Embedding generation integrated into background scan and bulk inserts</done>
</task>

<task type="auto">
  <name>Task 4: Implement /semantic-search GET endpoint</name>
  <files>
    - backend/main.py
  </files>
  <read_first>
    - backend/main.py
  </read_first>
  <action>
    In `backend/main.py`:
    - Add route `@app.get("/semantic-search")` accepting query `q: str` and limit `limit: int = 10`.
    - Generate query embedding using `get_embedding(q, is_query=True)`.
    - Fetch all database records that have vector embeddings.
    - Calculate similarity for each and return top matches sorted descending by score.
  </action>
  <verify>
    Verify top matches correlate with query semantics.
  </verify>
  <acceptance_criteria>
    - /semantic-search route exists.
    - Returns sorted matches with scores.
  </acceptance_criteria>
  <done>Semantic search endpoint exposed</done>
</task>

<task type="auto">
  <name>Task 5: Implement /folder-summary GET endpoint</name>
  <files>
    - backend/main.py
  </files>
  <read_first>
    - backend/main.py
  </read_first>
  <action>
    In `backend/main.py`:
    - Add route `@app.get("/folder-summary")` accepting path `folder_path: str`.
    - Query database records starting with/matching the path.
    - Compile summary prompts (counts, categories, metadata, alerts) and feed to `ask_ai()`.
    - Return the generative folder summary text.
  </action>
  <verify>
    Verify response yields a descriptive summary.
  </verify>
  <acceptance_criteria>
    - /folder-summary route exists.
    - Returns Gemini LLM summary text.
  </acceptance_criteria>
  <done>Folder summaries endpoint exposed</done>
</task>

<task type="auto">
  <name>Task 6: Create test_semantic_search.py and verify correctness</name>
  <files>
    - backend/test_semantic_search.py [NEW]
  </files>
  <action>
    Create unit and integration test suite `backend/test_semantic_search.py` verifying all aspects of cosine calculation, embedding formatting, database query, and endpoints.
  </action>
  <verify>
    Run test suite: `venv\Scripts\python.exe -m unittest backend/test_semantic_search.py`
  </verify>
  <acceptance_criteria>
    - All tests in `test_semantic_search.py` pass.
  </acceptance_criteria>
  <done>Semantic search test suite implemented and all tests pass</done>
</task>
</tasks>

<verification>
- [ ] Test suite `backend/test_semantic_search.py` executes successfully.
</verification>

<success_criteria>
- Vector embeddings are correctly saved inside SQLite database.
- Semantic search yields accurate matches with scores.
- Folder summary summarizes directory contents via Gemini LLM.
</success_criteria>

<output>
Create `.planning/phases/06-semantic-search-ai-summaries/06-01-SUMMARY.md` on completion.
</output>
